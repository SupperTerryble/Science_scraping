---
description: Run development tests automatically
---

This workflow runs all tests in the project using unittest. It is configured to run automatically (turbo mode).

// turbo-all
1. Run all tests
python3 -m unittest discover tests
